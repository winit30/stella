import {dpToPx} from "../../utils/utility";

const styles = {
    toolbarContainer: {
        height: dpToPx(26),
        backgroundColor: "#286F8B",
        justifyContent: "center",
        paddingLeft: dpToPx(8)
    },
    toolbarTitle: {
        color: "#ffffff",
        fontSize: dpToPx(12),
        fontWeight: "500"
    }
};

export default styles;
